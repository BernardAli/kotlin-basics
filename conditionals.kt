fun main() {
    var personHeight1 = 170
    var personHeight2 = 170

    if (personHeight1 > personHeight2) {
        println("use raw force")
    } else if (personHeight1 == personHeight2){
        println("use your power 9521")
    }
    else {
        println("use techniques")
    }

    var age = 40

    if (age >= 21) {
        println("You can do everything")
    }
    else if (age >= 18) {
        println("You can vote and drive")
    }
    else if (age >= 16) {
        println("You can drive")
    }
    else {
        println("You are only a child")
    }

    val isRainy = true

    if(isRainy) {
        println("It's rainy")
    }
}